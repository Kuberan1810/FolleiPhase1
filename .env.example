# Leave empty in development: vite.config.ts proxies /api to FastAPI, so the
# browser stays same-origin. Set this to the backend's origin for a deployed
# build (e.g. https://api.follei.com) -- no trailing slash, no /api suffix.
VITE_API_BASE_URL=
